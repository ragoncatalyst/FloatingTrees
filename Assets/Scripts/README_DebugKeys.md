# Debug keys / debug helpers: REMOVED

This file previously documented runtime debug keys and test-only helpers. Those debug/test features have been removed from runtime and are no longer supported.

If you need developer-only shortcuts again, please add an Editor-only utility or a build‑time flaged implementation.
